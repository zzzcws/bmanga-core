# Public-core dependency maintenance — 2026-09-12

This maintenance round starts from public revision
`7ba683846424c2cdfda876ddab8c7a3eaa44fb27`. It changes dependency and build
evidence, not reader features, database schemas, or the public-core boundary.
No deployment data, private configuration, provider integration, or real media
is included. Reviews below are maintainer-delegated AI technical reviews, not
independent legal opinions.

## Reviewed updates

| Component | Previous | Maintained |
| --- | --- | --- |
| Go toolchain | 1.26.6 | 1.26.8 |
| modernc.org/sqlite | 1.57.0 | 1.58.0 |
| modernc.org/libc | 1.74.4 | 1.75.6 |
| modernc.org/memory | 1.11.0 | 1.12.1 |
| @vitejs/plugin-react | 6.1.0 | 6.1.1 |
| @types/react-dom | 19.2.4 | 19.2.7 |
| @types/node | 24.13.3 | 24.13.4 |
| anchore/sbom-action | 0.24.0 | 0.24.2 |
| actions/attest-build-provenance | 3.0.0 | 4.2.2 |

The SQLite update includes its required libc and memory versions. The exact
Linux/amd64 import/linkage and optional-package exclusions are recorded in
`LICENSES/reviews/sqlite-v1.58.0-linux-amd64-technical.json`. Go remains on the
supported 1.26 patch line. The proposed Go 1.27.1 image-only update needs a
coordinated toolchain/profile migration and is not adopted in this round.

Node remains at the digest-pinned 24.19.0 builder. Node 26 type definitions
would describe a different runtime API surface, so the Node 24 patch is used
instead. Dependabot continues to propose Node 24 type updates; the major
version exclusion must be revisited with any Node builder migration.

Both updated GitHub Actions use verified full commit pins. The provenance
action's new optional storage-record feature is explicitly disabled, and all
workflow permissions remain unchanged. SBOM generation retains disabled
automatic dependency snapshots and release-asset uploads. References:
[SBOM action release](https://github.com/anchore/sbom-action/releases/tag/v0.24.2)
and [provenance action release](https://github.com/actions/attest-build-provenance/releases/tag/v4.2.2).

## Local verification

- All Go packages pass tests and vet; govulncheck v1.7.0 reports no reachable
  vulnerabilities in the checked graph.
- Both Linux/amd64 entrypoints cross-build and retain ten external module
  identities. The optional SQLite vec package has no imported or linked
  symbols in either inspected binary.
- On exact Node 24.19.0, all 167 frontend tests, type checking, production
  build, and full npm audit pass; npm audit reports zero vulnerabilities.
- An independent archive of the baseline revision and this updated frontend,
  each installed from its own lockfile, produce byte-identical sets of 21 web
  assets, including gzip and Brotli variants. This is a same-host comparison,
  not a cross-platform reproducible-build claim.
- The three changed frontend packages retain byte-identical MIT license texts;
  their changes introduce no additional shipped runtime component. React,
  React DOM, Scheduler, Vite, and Rolldown artifact versions remain unchanged.

Passing these checks does not prove absence of defects. Linux CI, exact-image
inspection, smoke testing, SBOM generation, provenance, and signature checks
remain required before promoting a new container tag. The release record is
the source of final revision, digest, and workflow evidence.
