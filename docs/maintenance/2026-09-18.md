# Public-core dependency maintenance - 2026-09-18

This round starts from public revision
`d31315ff9a7f9fd64fd9914ec450e7c44b274d34`. It consolidates dependency proposals
#41, #46, #47, #50, #51 and #52 into a coherent, tested artifact profile. It
does not change database schemas, provider support, source-library permissions
or the public-core boundary. Tests use synthetic fixtures only.

## Updates and review

| Component | Previous | Maintained |
| --- | --- | --- |
| Go toolchain | 1.26.8 | 1.27.1 |
| golang.org/x/image | 0.45.0 | 0.46.0 |
| golang.org/x/text | 0.41.0 | 0.42.0 |
| golang.org/x/sys | 0.47.0 | 0.48.0 |
| React / React DOM | 19.2.8 | 19.3.0 |
| @types/react | 19.2.18 | 19.3.0 |
| @types/react-dom | 19.2.7 | 19.3.0 |
| Scheduler | 0.27.0 | 0.28.0 |
| Vite | 8.2.2 | 8.3.0 |
| Rolldown | 1.2.4 | 1.2.9 |

React and its renderer now move together with matching type definitions. A
Dependabot group preserves that relationship for future version proposals.
The existing integrity gates correctly rejected dependency-only changes that
left the reviewed license inventory stale; none of those gates is relaxed.

The Go 1.27 migration previously deferred in the September 12 review is now
coordinated across go.mod, the Docker Official Image digest, exact toolchain
guards, CI and the reviewed artifact profile. The pinned index contains the
Linux/amd64 manifest recorded in the SQLite boundary evidence. SQLite 1.58.0,
libc 1.75.6 and memory 1.12.1 are unchanged. The optional SQLite vec package
remains outside both entrypoints and the scratch runtime.

Original license and patent files for all 16 mapped components are byte-identical
to the preceding profile. The inventory is regenerated from the verified module
cache, actual Go binary linkage and locked npm installation. Versioned source
locators and review hashes are refreshed together. These are maintainer-delegated
AI technical reviews, not independent legal opinions. Node stays at exact
24.19.0; its type-only package versions remain unchanged.

Upstream references: [Go 1.27.1 source](https://github.com/golang/go/tree/go1.27.1),
[React 19.3.0](https://github.com/facebook/react/releases/tag/v19.3.0), and
[Vite 8.3.0](https://github.com/vitejs/vite/releases/tag/v8.3.0).

## Verification

- All Go packages pass tests and vet on Go 1.27.1; govulncheck v1.7.0 reports
  no vulnerabilities in the checked graph.
- The license checker cross-builds both Linux/amd64 entrypoints, verifies the
  exact ten linked external modules and original notices, and inspects symbols
  to ensure the optional SQLite vec package remains excluded.
- On exact Node 24.19.0, all 184 frontend tests, type checking, the production
  build and npm audit pass. The audit reports zero vulnerabilities.
- All 68 publication-safety and license-integrity regression tests pass.
- Synthetic mobile-browser checks cover progressive details, failed-progress
  retry, stale-session fencing, focus/scroll stability, narrow layouts and
  long-strip rendering. No personal library or deployed database is used.

The local downloaded Chromium could not be spawned on this Windows host;
browser smoke tests use the separately launched installed Chrome in an isolated
test profile instead. Linux CI still installs its pinned test browser. Final
container inspection, runtime smoke, SBOM, provenance and signatures remain
mandatory independent release gates. The release record supplies their exact
source revision, image digest and workflow evidence; passing local checks does
not by itself prove the final container or absence of defects.
